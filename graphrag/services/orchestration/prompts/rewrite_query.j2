{# version: 1 #}
The current search query did not retrieve enough relevant context to answer the user's original
question. Rewrite the search query to improve retrieval while preserving the original intent.

Original question: {{ question }}
Current search query: {{ active_query }}

Produce a rewritten query (broaden, narrow, or rephrase as needed — do not change what is being
asked) and a short note on what changed and why.
